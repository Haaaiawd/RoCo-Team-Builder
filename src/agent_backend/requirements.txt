# RoCo Agent Backend - Python Dependencies
#
# 本文件被 Docker 镜像构建 (src/agent_backend/Dockerfile) 使用。
# openai-agents 最小下限为 0.14.0，对应 Runner.run_streamed /
# RawResponsesStreamEvent 的现代形态 —— agent_backend.runtime.chat_runtime
# 依赖这些 API，低版本会直接 ImportError。
# pyproject.toml 同步要求 >=0.14.0，避免本地 pip install -e . 落到旧版本。

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0

# OpenAI Agents SDK
openai-agents>=0.14.0
openai>=1.40.0

# HTTP Client
httpx>=0.27.0

# Caching
cachetools>=5.3.0

# 模糊匹配（data-layer 需要）
rapidfuzz>=3.6.0

# 模板渲染（spirit-card 需要）
jinja2>=3.1.0

# 配置加载
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
